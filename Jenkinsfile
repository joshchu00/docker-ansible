@Library("library") _

build {
  [
    gitURL = 'https://github.com/joshchu00/docker-ansible.git',
    gitBranch = 'master',
    buildLanguage = '',
    buildImage = '',
    dockerName = 'joshchu00/ansible:latest-alpine'
  ]
}
